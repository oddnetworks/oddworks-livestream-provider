#!/usr/bin/env node
'use strict';

const cli = require('../cli');

cli.main()
	.then(code => {
		if (code) {
			process.exit(code);
		}
		console.log('');
	})
	.catch(err => {
		console.error('CLI execution error');
		console.error(err.stack || err.message || err);
		process.exit(1);
	});
